---
description: MemoryAgent sidecar — recall before every reply
alwaysApply: true
---

# MemoryAgent 侧车

MCP 已配置时，`user_id` / `project_id` 由工作区目录名自动推导（无需手填）。

## 每轮流程

1. **回答前**：`memory_recall(query=用户当前问题)`；可省略 `user_id`、`project_id`。
2. **回答时**：只引用 recall 结果；标记陈旧的记忆先核实。
3. **回答后**：用户明确偏好/禁忌/项目决策/链接 → `memory_store`（`user` | `feedback` | `project` | `reference`）。
4. 需要整段上下文：`memory_export` → 使用返回的 `prompt_block`。
5. 看到 `ref=ccr_…`：`memory_retrieve_blob(ref_id)` 取回完整 tool 输出。

## 安装（本仓库未装时）

在 MemoryAgent 仓库根目录执行：

```bash
bash scripts/install-sidecar.sh /path/to/your/project
```

一条命令写入 `.cursor/mcp.json` + 本规则。
